# Core
WPLACE_URL=https://wplace.live/
HEADLESS=true
CHROME_NO_SANDBOX=true

# Image to draw (PNG/JPG). For testing, host your image somewhere or use raw GitHub.
IMAGE_URL=https://example.com/your-image.png
# Top-left anchor where to start placing pixels on the WPlace board
TARGET_X=100
TARGET_Y=100

# Palette: provide CSS hex colors used by the site palette (order matters)
PALETTE=#000000,#FFFFFF,#FF0000,#00FF00,#0000FF

# Pace / retries
PLACE_INTERVAL_MS=1500
MAX_ERRORS_BEFORE_RELOAD=10

# Optional: if the site requires login via cookie/session
#COOKIE_JSON=[{"name":"session","value":"abc","domain":"wplace.live","path":"/"}]
